# Red adjudication handoff

run_id: `{{ .RunID }}`
task_id: `{{ .TaskID }}`

blocker: `{{ .Blocker }}`
high_risk: `{{ .HighRisk }}`
disagreement: `{{ .Disagreement }}`
degraded: `{{ .Degraded }}`
unresolved_required_roles: `{{ .UnresolvedRequiredRoles }}`
operator_report_text: `{{ .OperatorReportText }}`
premium_escalation: `{{ .PremiumEscalation }}`

## Trigger Summary

{{ .TriggerSummary }}

## Evidence Pointers

{{ .EvidencePointers }}

## Requested Red Decision

{{ .RequestedRedDecision }}

## Boundary Notes

- Red adjudication resolves risk and disposition; reviewer consensus is not
  authority.
- Premium escalation requires explicit approval before Codex or Claude review.
- Required role coverage remains unresolved when primary and secondary provider
  candidates both fail; Red must not treat undeclared tertiary or premium output
  as clean coverage.
