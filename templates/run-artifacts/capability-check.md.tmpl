# Capability check

Status: complete

Run: `{{ run_id }}`
Backend Type: `{{ backend_type }}`
Adapter Type: `{{ adapter_type }}`
Selected backend: `{{ backend_type }}/{{ adapter_type }}`
Prompt profile: `{{ prompt_profile }}`
Tested CLI: `{{ tested_cli }}`
Last verified: `{{ last_verified }}`

## Task requirements

- write:
- approval control:
- question flow:
- retained events:
- stream observation required:
- plan lifecycle:
- observability:
- restart/recovery sensitivity:
- execution mode:

## Backend capability snapshot

- write:
- approval control:
- question flow:
- retained events:
- stream observation:
- plan lifecycle:
- observability:
- restart/recovery:

## Policy checks

- project backend policy:
- compatibility matrix status:
- user preference applied after gates: `<yes|no|not-provided>`
- planned/degraded backend restrictions:
- plan approval separate from tool/file/command approval:
- explicit post-approval start required: `<yes|no|not-applicable>`
- permission/input preconditions:
- backend caveats accepted:

## Verdict

`PASS|FAIL|BLOCKED`

## Notes
