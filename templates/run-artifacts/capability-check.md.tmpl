# Capability check

Status: complete

Run: `{{ run_id }}`
Backend Type: `{{ backend_type }}`
Adapter Type: `{{ adapter_type }}`
Selected backend: `{{ backend_type }}/{{ adapter_type }}`
Prompt profile: `{{ prompt_profile }}`
Tested CLI: `{{ tested_cli }}`
Last verified: `{{ last_verified }}`

## Task requirements

- write:
- approval control:
- question flow:
- retained events:
- stream observation required:
- plan lifecycle:
- observability:
- restart/recovery sensitivity:
- execution mode:

## Backend capability snapshot

- write:
- approval control:
- question flow:
- retained events:
- stream observation:
- plan lifecycle:
- observability:
- restart/recovery:

## KAH graph capability snapshot

- effective helper version:
- `kkachi-agent-helper capabilities --json` graph support:
- `kkachi-agent-helper graph --help` available:
- required graph commands present:
  - init --from-template:
  - validate:
  - explain:
  - diff/propose/apply/export when needed:
- selected template id/path, if graph init is used:
- graph validate/explain evidence path, if project graph is used:
- graph evidence artifact path, if graph affects this run: `graph-evidence.md`
- missing-capability gap record, if graph support is absent:
- missing graph policy: run-local phase evidence only; no `kah graph` alias and no direct `.kkachi-workflow.yaml` edits

## Policy checks

- project backend policy:
- compatibility matrix status:
- user preference applied after gates: `<yes|no|not-provided>`
- planned/degraded backend restrictions:
- plan approval separate from tool/file/command approval:
- explicit post-approval start required: `<yes|no|not-applicable>`
- permission/input preconditions:
- backend caveats accepted:

## Verdict

`PASS|FAIL|BLOCKED`

## Notes
