schema_version: kas-workflow-dispatch-packet/v1
strict_order: true
workflow_id: "{{workflow_id}}"
workflow_file: "{{workflow_file}}"
run_id: "{{run_id}}"
instance_id: "{{instance_id}}"
instance_revision: {{instance_revision}}
node_id: "{{node_id}}"
owner_role: "{{owner_role}}"
execution_lane: "{{execution_lane}}"
required_inputs:
  - "{{required_input}}"
expected_artifacts:
  - "{{expected_artifact}}"
prompt_ref: "{{prompt_ref}}"
approval_required: {{approval_required}}
fallback_policy: "none_fail_closed"
verification_gate: "{{verification_gate}}"
node_contract_source: "{{node_contract_source}}"
node_contract_ref: "{{node_contract_ref}}"
source_checksum: "{{source_checksum}}"
selector_registry_source: "{{selector_registry_source}}"
selector_registry_checksum: "{{selector_registry_checksum}}"
selector_match: "{{selector_match}}"
task_class: "{{task_class}}"
labels:
  - "{{label}}"
changed_surfaces:
  - "{{changed_surface}}"
required_capabilities:
  - "{{required_capability}}"
expected_start_revision: {{expected_start_revision}}
ready_node_reasons:
  - "{{ready_node_reason}}"
completion_authority: kah_only
stage1_direct_codex_is_kab_native_codex: false
direct_kah_state_write: false
