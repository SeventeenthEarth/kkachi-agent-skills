# Premium MAR Reviewer Request

Run ID: `{{ .RunID }}`
Task: `{{ .TaskID }}`
Approval evidence: `{{ .PremiumApprovalEvidence }}`

Perform a read-only review only when explicit approval for the premium reviewer
is present in the task contract or approval evidence.

## Inputs

Task contract:

{{ .TaskContract }}

Diff summary:

{{ .DiffSummary }}

## Rules

- Do not edit files.
- Do not run tests.
- Do not run linters.
- Do not run builds.
- Do not run installs.
- Do not run package managers.
- Do not start services.
- Do not run network probes.
- Do not run runtime verification commands.
- Do not mutate auth, tokens, provider settings, gateway config, model config,
  profile config, or runtime state.
- provider execution is not completion evidence.
- If approval evidence is absent or ambiguous, return `BLOCKED` and do not
  perform premium review.

## Output

Return concise findings with severity, evidence, and recommendation. Use
`PASS`, `PASS_WITH_FINDINGS`, `REQUEST_CHANGES`, `BLOCKED`, `DEGRADED`, or
`FAILED`.
