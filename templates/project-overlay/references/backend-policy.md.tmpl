# Backend policy

This backend summary is advisory. If it conflicts with `kkachi-agent-bridge/docs/public/compatibility-matrix.md` or `registries/cli-capabilities.yaml`, the compatibility matrix and synchronized registry win.

## Source ledgers

- `kkachi-agent-bridge/docs/public/compatibility-matrix.md`
- `kkachi-hermes-skills/registries/cli-capabilities.yaml`
- project overlay backend constraints

## Project-specific constraints

- default production-write backend set: `claude`, `glm`, `codex`, `gemini`, `opencode`
- forbidden backend states:
- required capabilities:
- approval model constraints:
- question/answer flow constraints:
- retained event constraints:
- observability constraints:

## Notes

Keep volatile backend maturity details here, not in the main `SKILL.md`.
