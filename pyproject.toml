[project]
name = "kkachi-hermes-skills"
version = "0.1.0"
description = "KAS skill-pack discovery CLI"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
kkachi-hermes-skills = "kkachi_hermes_skills.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
